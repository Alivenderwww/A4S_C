用户要求对重新修改的 C3.3 做**客观评分**，且明确要求「只按 .md 评分逻辑，不按已有测试」。这是评分/诊断任务，必须实跑验证（特别是数值对齐 F4 和 F2/F3 缩减率），不能仅凭静态分析。

我已完成只读分析，看清了这次 3 处改动：
1. `_annotate_gemm_bias`：把单个带 bias 的 Gemm 注解为 FusedMatMulBias（不改图）
2. `prefuse_conv_bn`：已实现并接入 pipeline，ResNet 分支检测 conv 带非零 bias 即报 FusedConv2dBatchNorm 注解（ResNet 无 BN 节点，真实折叠逻辑走不到）
3. matcher 顺序保持（EW 链优先）

待执行的评分步骤（写一个全新独立评判脚本，逐条按 spec §F1 触发条件核验，不复用已有测试）：
- F1：对每条 fusion_log 记录核验真实算子构成是否满足 spec 触发条件（疑点：Gemm 单算子注解、Conv-BN 未做反向折叠——这两点 spec 是否认）
- F2：launch 缩减率 min((raw−opt)/raw×5, 3)
- F3：buffer 缩减率 min((raw−opt)/raw×5, 3)
- F4：4 项结构检查 + MockRuntime 数值对齐（prefuse_conv_bn 改了 conv 权重，必须验证不破坏 F4）

退出 plan mode 后我会用一个独立评判脚本（从 stdin 或临时文件运行）实跑两个官方评测模型，给出 F1–F4 分数和总分。