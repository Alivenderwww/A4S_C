/public
/私钥

# remote_exec 使用的 SSH 私钥（按 id 命名，绝不提交）
/mig[0-9]*
/mig[0-9]*.pub

# Python 字节码
__pycache__/
*.py[cod]

# Orchestrator deepwork progress (local only)
.slim/deepwork/

# 提交打包产物（pack_trackc.py 生成；libaec.so 由脚本远程构建回传，zip 是提交压缩包）
/TrackC-*.zip
/C2/libaec.so
