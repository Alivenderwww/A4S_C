# C1 编译器 · 赛题深度分析与实现流程

> **⚠️ 本文档是历史规划分析，部分描述已过时。**
> 当前实现状态以 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md)（工程状态唯一事实源）为准。
>
> 基于精读 AEC ISA（`public/Track-B/spec.md` §15-16 + C2 `docs/03` + `golden/b_isa_public.json`）、全部 5 个公开 PTX、以及现有脚手架（`C1/`）重新推导。
> 本文保留有价值的历史分析、约束推导和工业参考，但**具体实现状态请以审计文档为准**。

---

## 0. 一句话定位

把 **真实 NVIDIA PTX 子集**（`.version 7.0 / sm_70`）编译成 **AEC 128-bit 定长机器码 `.aecbin`**，正确性是门禁，周期数（`total_cycles`）越低性能分越高。

**评分（100）**：正确性 50（按类别加权 **T1×4 + T2×8 + T3×10 + T4×12 + T5×16**）+ 性能 35（T2:5 / T3:9 / T4:10 / T5:11）+ 鲁棒 5 + Agent 10。
**性能归一化**：`r=T_base/T_你`，`p(r)=clip((log r−log0.5)/(log2−log0.5),0,1)` → 追平基线 50%、2× 满分。

---

## 0.5 组委会答疑澄清（重要，来自答疑记录）

- ⚠️ **输入格式可能从真实 NVIDIA PTX 改为统一的"AEC-IR"语法**。助教原话：当前公开题是 PTX 语法，但"为降低难度、统一格式、评分更公平，可能改用统一的 aecir 语法，后面在群里发布"。**→ parser 要保持模块化（词法/语法/语义分层），别把 NVIDIA-PTX 特有怪癖写死；盯群通知。** 好消息：AEC-IR 大概率比真实 PTX 更规整、更好解析。
- ✅ **golden/cycle model 确认不提供**（是赛道 B 交付物，给了等于泄题）。助教明确说"**这三个 model 大家也可以自己写一个作为自测用**" → 历史设计目标：自建 `sim/` 作功能 oracle（当前状态见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md) §二）。后续**可能**放出一个简单的二进制 C-model 作参考。
- **C1 看重的是"产出满足 GPGPU 约束的合法编译代码"**；cycle 由他们的模拟系统评估；每条指令执行时间是否公开待定（对 C1 "并不是很看重"）。
- **无实时榜单**；按**固定阈值**给分（正确率、加速比阈值预先定好），达标即满分，非相对排名。
- **开发期可联网、正常调用 API**（含 Agent 用 LLM API）；**只有最终评测在无网络环境**——所以 Agent 优化循环最终要能离线自足运行。
- 机房环境：**GCC 13.3 / Python 3.12 / CUDA 12.8**（+ numpy/cupy/torch）。

---

## 1. ⚠️ 五个必须先知道的硬约束（踩中任何一个 = 正确性直接挂）

### 1.1 编码 numbering 有两套，必须用 C 赛道这套（**不是** Track-B spec.md §A.1）
`Track-B/spec.md` 的 §A.1 opcode 表 和 §4 type 表，与 C 赛道实际用的编码**完全不同**：

| | Track-B §A.1/§4 | **C 赛道（正确）= `aec_isa.h` / `docs/03` / `b_isa_public.json`** |
|---|---|---|
| CPY | 0x0054 | **0x0051** |
| LOADI | 0x0055 | **0x0050** |
| CVTFF | 0x0050 | **0x0053** |
| type f32 | 0x8 | **0** |
| type f16 | 0xa | **2** |
| TMUL | 不存在 | **0x0060** |

验证：golden `CPY.u32 R1,%tid.x` → `word3=0x00510040`（opcode=0x0051），`ADD.f32 @P3` type 位=0。**编码唯一事实源：`aec_isa.h` + `b_isa_public.json`（C 赛道编号）；当前 bit-exact 状态见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md)。**
👉 **用法则：指令语义查 Track-B spec.md（§15-16 行为定义是权威且通用的），但二进制编码只认 `aec_isa.h` + `b_isa_public.json`。** 这条务必写进组委会 confirm 清单（C1 无独立 golden，编码歧义是最大风险）。

### 1.2 AEC 没有 64-bit 整数加法 —— 地址运算要塌缩成 32-bit
`ADD/SUB/MUL` 合法类型只有 `.u32/.s32/.f32/.f16/.bf16/.f64`，**没有 `.u64/.b64`**。但所有 PTX 都用 `add.u64`/`mul.wide.u32` 做地址。
关键事实：**ISA 规定"所有架构地址均为对应 space 内的 unsigned 32-bit byte offset"**（Track-B §5 / §8.1）。所以：
- `ld.param.u64` 取到的基址，其**低 32 位就是 gmem byte offset**，高 32 位无意义。
- `mul.wide.u32 %rd, %r, 4`（32×32→64）对现实索引不会溢出 32 位 → 当作 `MUL.u32`（取低 32）。
- `add.u64 %rd, base, off` → **`ADD.u32`（或 `.b32`）只算低字**。
👉 lowering 把所有指针类型 `.b64` vreg 当 32-bit 处理，只保留低字。`ADD.b64` 无合法编码（ADD 无 b64 类型），历史 lowering 设计为塌缩到 `ADD.u32`；当前状态见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md)。

### 1.3 divergent 分支 `@p bra` 不能直接编成 BRX —— 要 if-conversion 谓词化
AEC 是"launch 决定 active mask + lockstep"模型，**无 SIMT 重汇聚栈**。`BRX` 要求**所有 active lane 条件一致**（mixed → execution error）。
但 PTX 里的边界保护 `setp.ge.u32 %p, tid, n; @%p bra DONE;` 在 **N 不是 blockDim 整数倍**时是发散的（部分 lane 越界退出）→ 直接 `BRX` 会 execution error。
👉 **必须把边界保护做 if-conversion（谓词化）**：算出 `p = (tid < n)`，把 guard 之后到 `DONE` 的所有指令都加谓词 `@p`（谓词假的 lane 无副作用，天然正确）。这正是评分里的"**谓词执行优化**"。
- **循环回边**（`@p bra LOOP`，trip count 是编译期常量 32/16/K，各 lane 相同）→ **uniform，可以用 BRX**。
- 只有"线程发散的条件"才谓词化；uniform 的循环/跳转用 BR/BRX。
- PTX-01 N=4096÷256=16 整除，公开测试碰巧不发散；但**鲁棒性测试会改 N**（非整除）→ 不谓词化就挂。

### 1.4 f16 没有 2-byte load —— T5 的隐藏坑
`LD` 合法宽度只有 4/8 字节（`.b32/.b64/...`），**没有 `.u16`**。但 PTX-05 用 `ld.global.u16` 逐个取 f16 元素。
两条路：
- **标量回退**：`LD.b32`（4 字节含 2 个 f16）后按 `addr&2` 决定 `SHR 16` + `AND 0xffff` 抽出目标 f16，再 `CVTFF f32<-f16`。可行但繁琐，且要保证 4 字节不越界。
- **张量路径（推荐拿 perf）**：整段 K-loop 降到 `TLDA/TMUL/TSTA`，tile load 原生按 f16 处理，绕开逐元素 2-byte load。

### 1.5 `mad.f32` 应映射到 `FMA`（单次舍入），不是 `MAD`
sm_70 的 PTX `mad.f32` 是**融合乘加（单次舍入 = fma）**；而 AEC `MAD` 语义是"乘法先舍入再加法舍入"（Track-B §7.1），`FMA` 才是无限精度单次舍入。
👉 **PTX `mad.f32/fma.rn.f32` → AEC `FMA.f32`**；**PTX `mad.lo.u32` → AEC `MAD.u32`**（整数无舍入问题）。搞错 → GEMM 累加 fp 不 bit-exact → T5 正确性挂。

---

## 2. PTX 子集全集（parser 必须覆盖的指令）

5 个文件用到的**全部** PTX（隐藏测试是这些的变异，不会超纲太多）：

| PTX 指令 | → AEC lowering |
|---|---|
| `.version/.target/.address_size/.visible .entry/.param/.reg` + label + `{}` | 指令流/符号 |
| `ld.param.{u64,u32,f32}` | `LD.pmem.{b64→b32,u32,f32}`（低字；param block 按声明序自然打包） |
| `mov.u32 %r,%tid.x/%ctaid.x/%ntid.x/%ctaid.y` | `CPY.u32 Rd,%special` |
| `mov.{u32,f32} %r, imm` | `LOADI Rd, imm32` |
| `mad.lo.u32` | `MAD.u32` |
| `mul.wide.u32 %rd,%r,imm` | `MUL.u32`（低 32，需先 LOADI 常量） |
| `add.u64` / `add.u32` | `ADD.u32`（低字，见 1.2） |
| `mul.lo.u32` | `MUL.u32` |
| `and.b32 %r,%r,imm` / `shr.u32 %r,%r,imm` | `AND/SHR.u32`（**AEC 无立即数形式** → 先 `LOADI` 到寄存器再 reg-reg） |
| `setp.{ge,lt,...}.u32 %p,%r,%r` | `CMPP.{ge,lt}.u32 Pd,Rs1,Rs2` |
| `@%p bra LABEL` | 发散→谓词化；uniform 循环→`BRX P,->pc`（见 1.3） |
| `add/sub/mul.f32` | `ADD/SUB/MUL.f32` |
| `mad.f32` | **`FMA.f32`**（见 1.5） |
| `cvt.f32.f16 %f,%r` | `CVTFF`（dst f32, src f16） |
| `ld.global.u16` | 见 1.4（2-byte load 合成 或 走 TMUL） |
| `ld.global.f32` / `st.global.f32` | `LD.gmem.f32` / `ST.gmem.f32` |
| `ret` | `RET`（kernel 尾部；单 warp 模型下 RET/HALT 结束） |

**立即数是通用难点**：AEC 只有 `LOADI/LOADI64/BR/BRX/CALL` 带立即数，算术/逻辑/移位全是 reg-reg。→ 需要一个**立即数物化 pass**：把 PTX 里的常量（`4`、`31`、`0`、`16`、循环上界等）`LOADI` 到 vreg，常量传播 + CSE 再消掉重复。

---

## 3. 五个测试类别逐一拆解（考什么 / 什么 pass 拿分）

| 类别 | PTX | 正确性权重 | 性能权重 | 核心考点 | 拿分的 pass |
|---|---|---:|---:|---|---|
| **T1** 基础 lowering | PTX-01 vadd | 4 | 0 | 全流程跑通、编码合法 | parser→IR→encode，**谓词化边界**、**64位塌缩** |
| **T2** 控制/标量 | PTX-02 invariant_poly | 8 | 5 | 循环不变量、CSE、DCE | **LICM**（把 `a+b`/`a*b` 提出 32 次循环）+ **CSE**（`%f5==%f6`）+ **DCE**（`%f15` 死值）+ 常量传播 |
| **T3** 内存 | PTX-03 repeated_reuse | 10 | 9 | load 复用、SMEM 提升 | **LICM on load**（`[%rd5]` 地址循环不变 → 提出 16 次循环）+ 内存合并 + smem 缓存 |
| **T4** 寄存器/调度 | PTX-04 reg_schedule | 12 | 10 | 活跃区间、DDG、双发射 | **linear-scan 寄存器分配** + **list scheduling**（多个独立 load 提前发射、隐藏 32-cycle 延迟）+ **双发射配对** |
| **T5** Tensor/GEMM | PTX-05 gemm_f16 | **16** | 11 | TMUL 下降、tiling、多精度 | **GEMM 惯用法识别 → TLDA/TMUL/TSTA** + tiling + 多精度（9 种）+ 余数 tile |

**关键 ROI 观察**：
- **T5 权重 16（正确性）最高**。TMUL 可加 11 性能分。
- T2+T3 是**纯标量小 IR**，LICM/CSE/DCE 是教科书算法，性价比极高（14 性能分 + 18 正确性分）。
- 正确性是**perf 门禁**：任何类别编译结果跑错，该类别的性能分直接归零。所以 **correctness-first**。

---

## 4. 完整实现流程（分阶段 · 映射脚手架文件 · 对应知识库）

当前实现状态以 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md)（工程状态唯一事实源）为准，本文不再重复维护。以下仅保留历史阶段推导。

### Phase 1 — T1 基础 Lowering（历史目标设计）
历史目标设计：谓词化（`passes/pred_opt.cpp`）、64-bit 地址塌缩、立即数物化、`mad.f32→FMA`、param block 声明序自然打包。
当前实现状态详见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md) §三。

### Phase 2 — T2/T3 标量优化（历史目标设计）
历史目标设计：DCE（迭代到不动点，use-count + 副作用保护）、CSE（块内 LVN，迭代到稳定）、LICM（自然循环识别 + 不变量外提）、const_prop（LOADI 常量映射框架）、mem_coalesce（load/store 扫描计数）。
当前实现状态详见审计 §三。

### Phase 3 — T4 寄存器分配 + 调度（历史目标设计）
历史目标设计：`linear_scan.cpp`（全局活跃度数据流分析 + pair-aware 分配）、`list_sched.cpp`（DDG 构建 RAW/WAR/WAW + 关键路径高度优先列表调度 + 双发射计数）。
当前实现状态详见审计 §三、§五。

### Phase 4 — T5 GEMM→TMUL（历史目标设计）
历史目标设计：`codegen/gemm_tmul.cpp`——K-loop MAC 累加识别 → TMUL tile 下降 → 多精度参数化。
当前事实见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md) §三。

### Phase 5 — 鲁棒性 + Agent（历史目标设计）
历史目标设计：pred_opt + 变异覆盖 → 鲁棒性；Agent 闭环框架（6 候选配置 sweep、已知/未知输入口径区分）。
当前实现状态详见审计 §三、§五、§七。

---

## 5. AEC 功能模拟器（`C1/sim/`）

参见 [`C1/sim/README.md`](../C1/sim/README.md)。

**C1 不发 golden model / cycle model** —— AEC 功能模拟器构成：
- `aec_decode.py`：128-bit 解码
- `aec_sim.py`：32-lane warp lockstep 模拟器（256 GPR + 8 pred + gmem/pmem/smem/lmem）
- `cases.py`：5 公开 kernel 参数化 launch + numpy 独立参考
- `dogfood.py`：对拍驱动
- `mutate.py`：语义保持 PTX 变异
- `bench.py`：广泛 bench（覆盖 8 类变异变体）

回归结果详见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md) §二。

> 这解决了 Phase 1-4 改动无法本地验证正确性的问题。

---

## 6. 待向组委会确认（更新：部分已答疑）
1. **编码 numbering**（❗仍需确认）：C1 golden 用 Track-B §A.1 还是 C 赛道 `aec_isa.h`？（脚手架赌后者=`b_isa_public.json`；两者 opcode+type 都不同，赌错满盘皆输）
2. ~~是否提供 C1 golden/cycle model~~ → **已答疑：不提供，自己写自测 model（历史设计：自建 `sim/`，当前状态见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md)）；可能后续放二进制 C-model 参考。**
3. **param block 布局**（仍需确认）：声明序自然打包是否与 golden 一致？对齐规则？
4. **TMUL/张量指令编码**（仍需确认）：是否完全等同 `b_isa_public.json` / `docs/03`？非 16 倍数 tiling 的边界语义？
5. **`mad.f32` 语义**（仍需确认）：golden 按 fused(FMA) 还是 mul-then-add(MAD)？（影响 GEMM bit-exact）
6. **输入格式**（❗新增，最重要）：是否改用统一 AEC-IR？语法定义何时发布？—— 盯群，parser 保持模块化。

---

## 7. 行动建议

> 当前优先级详见 [`../docs/C1-完成度审计.md`](../docs/C1-完成度审计.md) §五，本文不重复维护。
