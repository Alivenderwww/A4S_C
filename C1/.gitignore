# Build artifacts
/obj/
/bin/
*.o
*.d
*.aecbin

# Sim / bench / agent working dirs (regenerated)
/sim/build/
__pycache__/
/agent/_agent_work/

# Submission entry points are copied from bin/ by `make submit` (agent/run_agent
# is a committed wrapper, so it is NOT ignored).
/compiler/
/disassembler/
