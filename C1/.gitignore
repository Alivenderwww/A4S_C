# Build artifacts
/obj/
/bin/
*.o
*.d
*.aecbin

# Sim / bench working dirs (regenerated)
/sim/build/
__pycache__/

# The submission-entry wrapper compiler/aec-cc is TRACKED (a committed script,
# not a build product); only the Windows test copy and the disassembler are not.
/compiler/*.exe
/disassembler/

/.venv
