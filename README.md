赛道C：编译器& Runtime &算子调度·总览

三子题各100分，覆盖从编译器到模型部署

C1 AECIR编译器  PTX风格IR→AECISA机器码；IR/SSA/CFG、指令调度、寄存器分配、多精度GEMM优化  正确性50 +性能35 +鲁棒5 +Agent10

C2 主机侧驱动 libaec.So：内存/Kernel/Stream/Event、10种GEMM、向量运算、虚拟设备驱动  Runtime30 + 计算库30 + Driver20 + Agent20

C3 算子调度  ONNX模型→AEC GPGPU推理；计算图解析、算子分解、融合、内存规划、端到端部署  图解析10+分解15+融合15+内存10+端到端50

赛道C1：AECIR编译器·赛题内容与评分标准

将 PTX 风格 IR 编译为 AEC ISA 机器码，支持优化 pass 与Agent自动调优

赛题内容
输入：PTX风格IR(.version/.target /.kernel /.reg /指令)
输出：合法AEC二进制+反汇编工具aec-Objdump
内部IR/SSA/CFG、基本块划分
•必需优化 pass：常量传播、死代码消除、公共子表达式消除、循环不变量外提、内存合并、谓词优化
•寄存器分配（256 个通用寄存器）、指令调度、双发射配对
•多精度GEMM优化与张量乘下降

评分标准
•A.编译与执行正确性：50分
T1×4+T2×8 +T3×10+T4×12 +T5×16
B.生成代码执行效率：35分
主指标：AEC周期模型总周期数。C.泛化与鲁棒性：5分
50道编译测试
D.Agent自动优化：10分性能分 8 +闭环完整性2

性能归一化公式
r_{g,i} = T^{base}_i / T_{g,i}
p(r) = clip(log r – log 0.5)/(log 2 – log 0.5), 0, 1)≤0.5倍→0%/1.0倍→50%/≥2.0倍→100%

Agent自动优化（10分）
Agent性能分（8分）
GM agent =(r _i^agent)^(1/10)
>=1.25倍满分
闭环完整性(2分）
独立运行/读取报告/重编译验证/生成报告

赛道C2：主机侧驱动与Runtime·赛题内容与评分标准
实现AEC 虚拟 GPGPU 的主机侧运行时，所有性能分使用虚拟周期
赛题内容
•运行时API：内存管理（分配/释放/拷贝）、内核启动、流/事件、设备查询
•计算库：10种GEMM精度+FP32向量乘加/点积/二范数
•虚拟设备驱动：设备ABI合规、双DMA通道、注册内存、故障恢复
•Agent（优秀等级）：dma_agent.py调度传输，kernel_agent.py 选择镜像
• 竞赛提供 34 个固定内核镜像，选手不可自定义

评分标准（总分100）
运行时 30分
设备查询/错误、内存管理、同步拷贝、流/事件、内核启动
计算库 30分
10种GEMM等权+向量运算
虚拟驱动 20分
ABI、双 DMA、注册内存、故障恢复
Agent 20分
DMA策略10+内核镜像选择10

固定平台
设备数量 1
设备内存 64MiB
DMA通道 2
最大线程/块 1024

等级要求
基础：查询/错误、内存、同步复制、向量加法、FP32/INT32 GEMM
良好：+流/事件、异步 DMA、注册内存、全部计算、故障恢复
优秀：+ 两个合法 Agent 并取得足够隐藏性能分

赛道C3：算子调度与模型部署·赛题内容与评分标准
将ONNX模型优化并部署到GPU上完成推理
赛题内容
•读取 ONNX模型并解析为有向无环图(DAG)
•将高层算子（矩阵乘、卷积、Softmax、层归一化等）分解为GPGPU内核序列并选择精度。
•算子融合：矩阵乘偏置、卷积+批归一化、逐元素链、Softmax+Dropout、残差+层归一化。内存规划：设备内存池、中间张量生命周期分析、权重预取、流级并行
•端到端部署：MNIST MLP、CIFAR-10 ResNet-18、Decoder-only Transformer

子任务 分值 评测方式 关键维度
C3.1计算图解析 10 命令行→DAG JSON   ONNX解析+节点/边导出
C3.2算子分解 15 微基准(MNIST MLP+CIFAR ResNet) D1多精度/D2 内核序列/D3中间张量/D4调优/D5硬件能力
C3.3 算子融合 15 微基准+数值对齐 F1模式覆盖（5）/F2内核启动数/F3缓冲数/F4正确性
C3.4内存规划 10 代码审查 A设备池/B生命周期/C碎片整理/D权重预取/E流级并行
C3.5端到端 50 端到端推理 精度门槛+准确率门槛 + 推理时间 25 +峰值显存 15

C3.3五个目标融合模式 (各1分）
融合矩阵乘加偏置：矩阵乘→加偏置
融合卷积批归一化：卷积→批归一化（需预融合，训练时已折进权重)融合逐元素链：2-5个相邻逐元素运算(加→乘→ReLU)融合Softmax Dropout： Softmax→Dropout
融合残差归一化：残差加→层归一化

ResNet训练时BN折进conv权重，导出ONNX无BN节点